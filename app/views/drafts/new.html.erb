<h1> Create A Draft </h1>

<div class="card card-body card-padding">
	<div>
		<%= form_tag "/drafts", multipart: true, method: :post do %>
			Name This Draft: <input class="form-control" name="name" placeholder="Draft Title"> <br> 
			<div class="field">
     		Upload a .docx	<%= label_tag :file %> . (It MUST be .docx. You can use google drive to convert your files if necessary). 
     		<%= file_field_tag :file %>
  		</div>
  		<br>
  		<br>
  		<br>
			Or you can just paste your chapter or draft here:
			<textarea class="form-control" rows="10" name="text" placeholder="Note: text here will override an upload."></textarea>
			<br>



			For Which Project: <%= collection_select(:drafts, :work_id, @current_user.works, :id, :name, prompt: "Select Work") %> 
			<br>
			<br>

			<input type="submit" value="create draft">
		<% end %>
	</div>

</div>

